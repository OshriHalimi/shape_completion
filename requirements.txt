# Step 1: Open up a conda virtual env for Python 3.7 named shape_completion 
# Step 2: Install torch for Python 3.7 and CUDA 10.1. For Windows: conda install pytorch torchvision cudatoolkit=10.1 -c pytorch
# Step 3: Install these on a conda env via pip
scipy==1.4.1
psutil==5.6.7
pytorch-lightning==0.5.3.2
open3d==0.9.0.0
torchviz==0.0.1
vtkplotter==2019.4.10
matplotlib==3.0.2
scikit_learn==0.20.2
torch-scatter==1.4.0
pillow==6.2.1
pyvista==0.23.0

# Step 4[ SKIP THIS if installing a bot computer ] : In order to support nn.visualize(), you must install Graphviz. For Windows: 
# 1. Open up your browser and go to: https://graphviz.gitlab.io/_pages/Download/windows/graphviz-2.38.msi
# 2. Install the application. Default install path is: C:\Program Files (x86)\Graphviz2.38
# 3. Add to your PATH variable: "C:\Program Files (x86)\Graphviz2.38\bin"
# Step 5: If using PyCharm - Make sure that 'src/core' is enabled as the Sources Root  - https://stackoverflow.com/questions/28705029/pycharm-error-no-module-when-trying-to-import-own-module-python-script
# Step 6: Make sure you have the dataset installed

